import { SearchReservationPipe } from './search-reservation.pipe';

describe('SearchReservationPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchReservationPipe();
    expect(pipe).toBeTruthy();
  });
});
